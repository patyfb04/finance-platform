name: Dev Workflow

on:
  push:
    branches: [dev]
  pull_request:
    branches: [features/*]

jobs:
  call-common-deploy:
    uses: ./.github/workflows/common-deploy.yml
    with:
      bun-version: "latest"
